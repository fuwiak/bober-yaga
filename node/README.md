# Legacy Node yaga

Superseded by the Go + Bubble Tea CLI in the parent directory.

Kept only so `search` / `gpt` bricks can reuse the Node HTTP helpers via `node/bin/yaga.js`.
Do not install this as the global `yaga` command.
